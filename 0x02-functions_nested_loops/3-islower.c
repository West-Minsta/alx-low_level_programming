#include"main.h"
/**
 * _islower - function name
 * Description: prints "action" when called
 * Return: 0
 * @c : character to check the case
 */
int _islower(int c)
{
return (c >= 97 && c <= 122);
}
